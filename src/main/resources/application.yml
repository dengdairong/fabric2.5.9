spring:
  application:
    name: fabric

logging:
  file:
    name: app.log
    path: ./logs
  level:
    root: WARN

fabric:
  chaincodeId: mycc3
  channel: mychannel
  contractNames: ManufacturerContract,HospitalContract,PatientContract
  path:

server:
  port: 8888
